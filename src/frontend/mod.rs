//! Frontend boundary for user interaction.
//!
//! This module should own terminal presentation, input handling, and debugger
//! workflows while treating the emulation core as an external dependency.

/// Placeholder for the terminal-facing frontend.
pub struct Frontend;

impl Frontend {
    /// Creates a frontend placeholder.
    pub fn new() -> Self {
        Self
    }
}
