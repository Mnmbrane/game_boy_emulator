//! CPU state and instruction execution.
//!
//! The CPU will eventually own registers, flags, interrupt entry, and the
//! fetch/decode/execute pipeline for the Game Boy instruction set.

/// Placeholder for the Game Boy CPU.
pub struct CPU;

impl CPU {
    /// Creates a CPU placeholder.
    pub fn new() -> Self {
        Self
    }
}
