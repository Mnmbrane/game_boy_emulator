//! DMA transfer state.
//!
//! This module is reserved for OAM DMA behavior and any transfer timing rules
//! that affect memory access and sprite data updates.

/// Placeholder for direct memory access state.
pub struct DMA;

impl DMA {
    /// Creates a DMA placeholder.
    pub fn new() -> Self {
        Self
    }
}
